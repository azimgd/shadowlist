{
  "// note": "Shared strictness baseline every package extends. Package tsconfigs keep their own target/lib/module/paths and override as needed.",
  "compilerOptions": {
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true
  }
}
