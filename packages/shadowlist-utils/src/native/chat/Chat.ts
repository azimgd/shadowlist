import { ChatList } from './ChatList';
import { ChatBubble } from './ChatBubble';
import { ChatInput } from './ChatInput';

export const Chat = {
  List: ChatList,
  Bubble: ChatBubble,
  Input: ChatInput,
};
