package com.shadowlist;

import android.content.Context;
import android.util.AttributeSet;
import com.facebook.react.views.view.ReactViewGroup;

public class SLContent extends ReactViewGroup {
  public SLContent(Context context) {
    super(context);
    init(context);
  }

  public SLContent(Context context, AttributeSet attrs) {
    super(context);
    init(context);
  }

  private void init(Context context) {
  }
}
